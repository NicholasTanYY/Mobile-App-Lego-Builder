export const handleLogin = async (navigation) => {
    navigation.navigate('Login');
};

export const handleSignup = async (navigation) => {
    navigation.navigate('Sign up');
};

export const handleFAQ = async (navigation) => {
    navigation.navigate('FAQ');
};